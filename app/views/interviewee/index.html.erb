<%# OVERRIDE: Blacklight v7.33.1 - Replaced default search sidebar with UCLA COHR promotional content and added tabbed informational sections and custom theming %>
<div class="container">
  <div class="u-heading-v2-5--bottom g-brd-primary ">  
    <h2 class="u-heading-v2__title text-uppercase g-font-weight-300 mb-0 g-mt-40">Browse Interviewees by Last Name</h2>
  </div>
  
  <% unless has_search_parameters? %>
    <div class="container">
      <div id="sidebar" class="container <%= sidebar_classes %>">
      <%= render 'search_sidebar' %>
    </div>
  <% else %>
    <div class="container">
      <div id="sidebar" class="container <%= sidebar_classes %>">
      <%= render 'search_sidebar' %>
    </div>
  <% end %>

  <div id="content" class="container <%= main_content_classes %>">
    <%= render 'search_results' %>
  </div>
</div>
